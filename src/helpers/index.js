const { getAllPagesText } = require ('./getAllPagesText.helper');
const { getFetch } = require ('./getFetchWCC.helper');
const { getRanking } = require ('./getRanking.helper');
const { getRespObj } = require ('./getRespObj.helper');
const { getSchedule } = require ('./getSchedule.helper');

module.exports = {
    getAllPagesText,
    getFetch,
    getRanking,
    getRespObj,
    getSchedule,
}